# Sessions 0.2.22

- Keeps the left Sessions navigator focused on the main conversations you are driving instead of expanding agent-created subagents into the list.
- Keeps every subagent available in its manager session's dedicated Subagents panel, including the action that promotes one into a main session.
- Removes the per-session expand chevrons, nested helper rows, and completed-child rollups that made pinned and live sessions difficult to scan.
- Prevents duplicate session identifiers from producing duplicate navigator rows while preserving the complete daemon record.
